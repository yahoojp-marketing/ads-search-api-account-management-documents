AccountManagementServiceEnterpriseTypePosition:
  type: string
  nullable: true
  enum:
  - FRONT
  - BACK
  - UNKNOWN
  title: AccountManagementServiceEnterpriseTypePosition
  description: |
    <div lang="ja">AccountManagementServiceEnterpriseTypePositionは、企業形態の位置を表します。
    <br>このフィールドは、ADD時に省略可能となり、SET時は無視されます。<br>※ADD時、enterpriseTypeがKOJIN（個人事業主）またはOTHER（その他法人）の場合は設定できません。<br>※clientTypeがSELFの時は、ADD時でも無視されます。</div>
    <div lang="en">AccountManagementServiceEnterpriseTypePosition describes the position of enterprise type.<br>This field is optional in ADD operation, and will be ignored in SET operation.<br>* Cannot be set when enterpriseType is KOJIN or OTHER in ADD operation.<br>* When clientType is "SELF", this filed will be ignored even in ADD operation.</div>
    <hr>
    <p>* <code>FRONT</code> - <span lang="ja">会社名の前です。</span><span lang="en">Enterprise type is placed before the company name.</span></p>
    <p>* <code>BACK</code> - <span lang="ja">会社名の後ろです。</span><span lang="en">Enterprise type is placed after the company name.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value.</span></p>
