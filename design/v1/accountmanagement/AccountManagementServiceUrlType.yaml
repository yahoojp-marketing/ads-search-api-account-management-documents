AccountManagementServiceUrlType:
  type: string
  nullable: true
  enum:
  - PC
  - DOCOMO
  - KDDI
  - SOFTBANK
  - OTHER
  - UNKNOWN
  title: AccountManagementServiceUrlType
  description: |
    <div lang="ja">AccountManagementServiceUrlTypeは、申し込みURL種別を表します。<br>このフィールドは、ADD時は必須となり、SET時は無視されます。</div>
    <div lang="en">AccountManagementServiceUrlType describes the type of application url.<br>This field is required in ADD operation, and will be ignored in SET operation.</div>
    <hr>
    <p>* <code>PC</code> - <span lang="ja">PCです。</span><span lang="en">PC.</span></p>
    <p>* <code>DOCOMO</code> - <span lang="ja">ドコモです。</span><span lang="en">Docomo.</span></p>
    <p>* <code>KDDI</code> - <span lang="ja">auです。</span><span lang="en">au.</span></p>
    <p>* <code>SOFTBANK</code> - <span lang="ja">ソフトバンクです。</span><span lang="en">Softbank.</span></p>
    <p>* <code>OTHER</code> - <span lang="ja">その他です。</span><span lang="en">other.</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value.</span></p>
